# topics/

Scenarios for read-only topic introspection: `GET /v1/topics` (list visible topics) and `GET /v1/topics/segments` (storage-backend segment manifest for a `(topic, partition)`). These are pure-introspection endpoints and MAY omit `## Side effects`.

See [../INDEX.md](../INDEX.md#topics--get-v1topics-get-v1topicssegments) for the scenario list.
